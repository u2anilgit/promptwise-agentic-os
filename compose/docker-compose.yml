name: promptwise-agentic-os

services:
  ollama:
    image: ollama/ollama:${OLLAMA_IMAGE_TAG:-0.3.12}
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${OLLAMA_PORT:-11434}:11434"
    volumes:
      - ollama_data:/root/.ollama

  qdrant:
    image: qdrant/qdrant:${QDRANT_IMAGE_TAG:-v1.11.0}
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${QDRANT_PORT:-6333}:6333"
    volumes:
      - qdrant_data:/qdrant/storage

  gateway:
    build:
      context: ..
      dockerfile: compose/gateway.Dockerfile
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${GATEWAY_PORT:-8420}:8000"
    depends_on:
      - ollama
      - qdrant
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/healthz')"]
      interval: 10s
      timeout: 3s
      retries: 5

volumes:
  ollama_data:
  qdrant_data:
